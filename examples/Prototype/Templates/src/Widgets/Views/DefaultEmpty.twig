{# builderInfo #}

<div class="alert alert-info" role="alert">
  This is a default view generated by the prototype builder.<br/>
  </br>
  In this TWIG template, a set of commented sample implementations for e.g.
  breadcrumbs, title, table, grid or form, is ready to use.</br>
  <br/>
  Uncomment them to start creating your custom view.
</div>

{# You can dump:
    - view's unique ID (uid),
    - view's parameters (based on, e.g. GET variables in URL),
    - application config, or
    - parameters of the window, if this view is renderd in the window.

  {{ dump(uid) }}
  {{ dump(viewParams) }}
  {{ dump(config) }}
  {{ dump(windowParams) }}
#}

{# Sample code to render breadcrumbs
  {{ include('ADIOS/Views/Breadcrumbs', {'items': [
    {'url': '', 'text': 'Home'},
    {'url': 'level-1', 'text': 'Level #1'},
    {'url': 'level-2', 'text': 'Level #2'},
    {'url': '', 'text': 'Level #3'}
  ]}) }}
#}

{# Sample code to render a include another view loaded asynchroneously
  <app-view
    controller="Campaigns/Statistics"
    eval-script-tags="1"
  ></app-view>
#}

{# Sample code to render table using Mui data grid component
  {% set model = adiosModel(viewParams.model) %}
  {% set tableParams = null %} <!-- set this tableParams, if you wish specific tableParams for this view -->
  {% set formParams = null %} <!-- set this formParams, if you wish specific formParams for this view -->

  <app-table
    model="{{ viewParams.model }}"
    tag="{{ viewParams.tag }}"
    endpoint="{{ viewParams.tableEndpoint | default(model.tableEndpoint) }}"
    {{ adiosHtmlAttributes(tableParams | default(viewParams.tableParams) | default(model.tableParams))|raw }}
    form-id="{{ viewParams.id }}"
    form-endpoint="{{ viewParams.formEndpoint | default(model.formEndpoint) }}"
    form-params='{{ (formParams | default(viewParams.formParams) | default(model.formParams))|json_encode|raw }}'
  ></app-table>
#}

{# Sample code to render form
  <app-form
    model="{{ viewParams.model }}"
    id="{{ viewParams.id }}"
  ></app-form>
#}