<?php

{{ builderInfo.php|raw }}

require("src/App.php");

$app = new App($config);

include(__DIR__ . '/src/Web/SiteMap.php');

echo $app->web
  ->setRouter(new \ADIOS\Core\Web\Router($siteMap))
  ->render()
;
