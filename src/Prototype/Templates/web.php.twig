<?php

{{ builderInfo.php|raw }}

require("src/App.php");

$adios = new App($config);

include(__DIR__ . '/src/Web/SiteMap.php');

echo $adios->web
  ->setRouter(new \ADIOS\Core\Web\Router($siteMap))
  ->render()
;
