<?php

define('ADIOS_WIDGETS_DIR', realpath(__DIR__ . "/../src/Widgets"));

// global
$config['application_name']              = "{{ ConfigApp.applicationName }}";
$config['session_salt']                  = "{{ ConfigApp.sessionSalt }}";

// dirs & urls
$config["dir"]                           = realpath(__DIR__ . "/../src");
$config["log_dir"]                       = realpath(__DIR__ . "/../log");
$config["tmp_dir"]                       = realpath(__DIR__ . "/../tmp");
$config['files_dir']                     = realpath(__DIR__ . "/../upload");
$config['files_url']                     = "//" . $_SERVER['HTTP_HOST'] . $config['rewrite_base'] . "upload";

// brand
$config["brand"]["title"]                = "{{ ConfigApp.applicationName }}";
$config["brand"]["favicon"]              = "{$config["url"]}/src/Assets/images/favicon.png";
$config["brand"]["login"]["splash"]      = "{$config["url"]}/src/Assets/images/login-screen.jpg";
$config["brand"]["sidebar"]["icon"]      = "{$config["url"]}/src/Assets/images/logo.png";
$config["brand"]["sidebar"]["title"]     = "";

// widgets
{% for widgetName, widgetConfig in Widgets %}
$config['widgets']['{{ widgetName }}']['enabled'] = TRUE;
{% endfor %}

// misc
$config['default_action'] = "{% if ConfigApp.defaultAction == "" %}Desktop{% else %}{{ ConfigApp.defaultAction }}{% endif %}";
$config['available_languages'] = ["sk"];

{% if ConfigApp.smtp %}
// SMTP server
$config['smtp_host'] = "{{ ConfigApp.smtp.host }}";
$config['smtp_port'] = "{{ ConfigApp.smtp.port }}";
$config['smtp_login'] = "{{ ConfigApp.smtp.login }}";
$config['smtp_password'] = "{{ ConfigApp.smtp.password }}";
$config['smtp_protocol'] = "{{ ConfigApp.smtp.protocol }}";
$config['smtp_from'] = "{{ ConfigApp.smtp.from }}";
{% endif %}
