<?php

namespace {{ thisAction.namespace }};

class {{ thisAction.class }} extends \ADIOS\Core\Widget\Action {
  public function render() {
    $uiComponent = $this->adios->ui->create(
      "{{ thisAction.config.uiComponentName }}",
{{ varExport(thisAction.config.uiComponentParams, "      ")|raw }}
    );

    return $uiComponent->render();
  }
}