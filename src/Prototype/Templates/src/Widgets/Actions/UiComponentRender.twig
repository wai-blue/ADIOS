<?php

namespace ADIOS\Actions\{{ thisWidget.name }};

class {{ thisAction.name }} extends \ADIOS\Core\Widget\Action {
  public function render() {
    return $this->adios->renderAction(
      "{{ thisAction.config.uiComponentName }}",
      {{ varExport(thisAction.config.uiComponentParams, "    ")|raw }}
    );
  }
}