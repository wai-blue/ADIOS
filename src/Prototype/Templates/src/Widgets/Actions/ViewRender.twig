<?php

namespace {{ thisAction.namespace }};

class {{ thisAction.class }} extends \ADIOS\Core\Widget\Action {
  public function render() {
    $view = $this->adios->ui->create(
      "{{ thisAction.config.viewName }}",
{{ varExport(thisAction.config.viewParams, "      ")|raw }}
    );

    return $view->render();
  }
}