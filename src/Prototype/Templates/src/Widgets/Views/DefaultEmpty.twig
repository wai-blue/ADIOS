{# builderInfo #}

<div class="alert alert-info" role="alert">
  This is a default view generated by the prototype builder.<br/>
  </br>
  In this TWIG template, a set of commented sample implementations for e.g.
  breadcrumbs, title, table, grid or form, is ready to use.</br>
  <br/>
  Uncomment them to start creating your custom view.
</div>

{# You can dump:
    - view's unique ID (uid),
    - view's parameters (based on, e.g. GET variables in URL),
    - application config, or
    - parameters of the window, if this view is renderd in the window.

  {{ dump(uid) }}
  {{ dump(viewParams) }}
  {{ dump(config) }}
  {{ dump(windowParams) }}
#}

{# Sample code for breadcrumbs
  <adios-breadcrumbs
    items="[
      {'url': '', 'text': 'Home'},
      {'url': 'level-1', 'text': 'Level #1'},
      {'url': 'level-2', 'text': 'Level #2'},
      {'url': '', 'text': 'Level #3'}
    ]"
  ></adios-breadcrumbs>
#}

{# Sample code for view
  <adios-view
    controller="Campaigns/Statistics"
    eval-script-tags="1"
  ></adios-view>
#}

{# Sample code for table
  <adios-table
    model="{{ viewParams.model }}"
    tag="{{ viewParams.tag }}"
    data-controller="{{ ADIOS_Core_Model_getDefaultTableParams(viewParams.model).dataController }}"
    params-controller="{{ ADIOS_Core_Model_getDefaultTableParams(viewParams.model).paramsController }}"
    form-id="{{ viewParams.id | default(null) }}"
    form-params="{{ ADIOS_Core_Model_getDefaultFormParams(viewParams.model)|json_encode|raw }}"
  ></adios-table>
#}

{# Sample code for form
  <adios-form
    model="{{ viewParams.model }}"
    id="{{ viewParams.id }}"
  ></adios-form>
#}