<?php

{{ builderInfo.php|raw }}

namespace {{ thisController.namespace }};

class {{ thisController.class }} extends \ADIOS\Controllers\Components\Table\Data {

  public static bool $hideDefaultDesktop = true;

  public function prepareQuery(): \Illuminate\Database\Eloquent\Builder {
    $query = parent::prepareQuery();

    // Modify the $query here
    // You can use $this->model property to get the model for which the query is built.
    // !!! DO NOT FORGET TO DELETE THE prototypeBuilderRegenerateAllowed TAG !!!

    return $query;
  }

  public function postprocessData(array $data): array {
    return $data;
  }

}
