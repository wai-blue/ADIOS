<?php

{{ builderInfo.php|raw }}

namespace {{ thisController.namespace }};

class {{ thisController.class }} extends \ADIOS\Controllers\Components\Table\OnLoadParams {

  public static bool $hideDefaultDesktop = true;

  public function renderJson() { 
    $parentTableParams = parent::renderJson();

    try {
      return array_merge($parentTableParams,
{{ varExport(thisController.config.params, "      ")|raw }} ?? []
      );
    } catch (\ADIOS\Core\Exceptions\GeneralException $e) {
      // TODO: Error
    }
  }
}
