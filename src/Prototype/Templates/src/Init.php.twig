<?php

{{ builderInfo.php|raw }}

ini_set("display_errors", 1);
ini_set("error_reporting", E_ALL ^ E_NOTICE ^ E_WARNING);

// load configs
require_once(__DIR__ . "/../ConfigEnv.php");
require_once(__DIR__ . "/ConfigApp.php");

// include autoloaders
require_once(__DIR__ . "/../vendor/autoload.php");

// create own ADIOS class
class App extends \ADIOS\Core\Loader {
  public function __construct($config = NULL, $mode = NULL, $forceUserLogout = FALSE) {
    parent::__construct($config, $mode, $forceUserLogout);

    require_once(__DIR__ . '/Routing.php');

  }

  public function hasPermissionsForController($action, $params = NULL) {
    return TRUE;
  }
}
