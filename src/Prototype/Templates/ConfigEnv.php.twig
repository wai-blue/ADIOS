<?php

{{ builderInfo.php|raw }}

define('GTP', "{{ ConfigEnv.db.globalTablePrefix }}");

// db
$config["db_host"]             = "{{ ConfigEnv.db.host }}";
$config["db_user"]             = "{{ ConfigEnv.db.user }}";
$config["db_password"]         = "{{ ConfigEnv.db.password }}";
$config["db_name"]             = "{{ ConfigEnv.db.database }}";
$config["global_table_prefix"] = "{{ ConfigEnv.globalTablePrefix }}";

// dirs & urls
$config['rewrite_base']        = "{{ ConfigEnv.rewriteBase }}";
$config["url"]                 = "{{ ConfigEnv.rootUrl }}";

// web
$config['web']['rewriteBase'] = '{{ ConfigEnv.rewriteBase }}web/';

// misc
$config['devel_mode'] = TRUE;
$config['language'] = "sk";
{% if ConfigEnv.skin %}$config["skin"] = "{{ ConfigEnv.skin }}";{% endif %}
