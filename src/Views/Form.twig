<adios-form
  model="{{ viewParams.model }}"
  id="{{ viewParams.id }}"
  title-for-inserting="{{ viewParams.titleForInserting }}"
  title-for-editing="{{ viewParams.titleForEditing }}"
  layout='{{ viewParams.layout|json_encode|raw }}'
  content='{{ viewParams.content|json_encode|raw }}'
  columns='{{ viewParams.columns|json_encode|raw }}'
></adios-form>
